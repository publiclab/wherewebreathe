@font-face {
    font-family: Junction;
    src: url(/fonts/junction/webfonts/junction-regular.woff);
}
body{
  padding-top: 70px;
  font-family: Junction,Helvetica Neue,Helvetica,Arial,sans-serif;
  height: 100%;
  background-color: #f8f8f8;
}
.content{
  margin-top: -20px;/*override navbar margin*/
  background-color: #fff;
  padding-bottom: 20px;
}
.footer{
  border-top: 1px solid #e7e7e7;
  clear: both;
  margin: 0;
  padding: 20px;
}
/*make form inputs and helper messages default to Helvetica et al.*/
.alert, input{
  font-family: Helvetica Neue,Helvetica,Arial,sans-serif;
}
.navbar-btn{
  margin-left: 5px;
}
#divLogin{
  padding:7px;
}
#vin, #hud {
  max-width: 600px;
  display: inline-block;/*makes the clickable area from the parent href fit to image*/
}

figcaption{
  font-style:italic
}
